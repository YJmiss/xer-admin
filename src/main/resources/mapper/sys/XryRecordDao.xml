<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oservice.admin.modules.sys.dao.XryRecordDao">

    <!--自定义分页查询-->
    <select id="pageList" parameterType="java.util.Map" resultType="java.util.Map">
        select a.*,b.title as courseTitle, b.id as courseId, c.title as videoTitle,c.id as videoId, d.username
            from  xry_record a
              left join xry_course b on  a.record_id=b.id
              left join xry_video c on a.record_id=c.id
              left join sys_user d on a.user_id = d.user_id  where 1=1
        <if test="params.type != null and params.type != ''">
            and a.type like #{params.type}
        </if>
        <if test="params.title != null and params.title != ''">
            and (b.title like #{params.title} or  c.title like #{params.title})
        </if>
        <if test="params.limit != null">
            limit 0,${params.limit}
        </if>
    </select>
    
</mapper>