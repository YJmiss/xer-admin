<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oservice.admin.modules.sys.dao.XryVideoDao">

    <!--查询返回的数据总数-->
    <select id="countTotal" parameterType="java.util.Map" resultType="java.lang.Long">
        select count(*) from xry_video a left join xry_course b on a.course_id=b.id left join xry_course_catalog c on a.catalog_id=c.id where 1=1
        <if test="params.status != null and params.status != '' and params.status == 1">
            and a.status = #{params.status}
        </if>
        <if test="params.title != null and params.title != ''">
            and a.title like #{params.title}
        </if>
        <if test="params.courseId != null and params.courseId != ''">
            and a.course_id = #{params.courseId}
        </if>
        <if test="params.catalogId != null and params.catalogId != ''">
            and a.catalog_id = #{params.catalogId}
        </if>
    </select>
    <!--自定义分页查询-->
    <select id="pageList" parameterType="java.util.Map" resultType="java.util.Map">
        select a.*,b.title as courseTitle,c.title as catalogTitle
        from xry_video a left join xry_course b on a.course_id=b.id left join xry_course_catalog c on a.catalog_id=c.id  where 1=1
        <if test="params.status != null and params.status != '' and params.status == 1">
            and a.status = #{params.status}
        </if>
        <if test="params.title != null and params.title != ''">
            and a.title like #{params.title}
        </if>
        <if test="params.courseId != null and params.courseId != ''">
            and a.course_id = #{params.courseId}
        </if>
        <if test="params.catalogId != null and params.catalogId != ''">
            and a.catalog_id = #{params.catalogId}
        </if>
        <if test="params.pageSize != null and params.pageSize != ''">
            limit ${params.pageNo},${params.pageSize}
        </if>
    </select>
    <!-- 修改视频的状态（视频审核） -->
    <update id="recordExamineInfo" parameterType="java.util.Map">
        update xry_video set status = #{params.status} where id = #{params.id}
    </update>

</mapper>